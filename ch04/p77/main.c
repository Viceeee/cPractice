#include <stdio.h>

#define dprint(expr) printf(#expr " = %d\n", expr)
#define paste(front, back) front ## back
int main()
{
    /*
    int hj = 10;
    dprint(hj);
    int paste(name,1);
    */
    return 0;
}
